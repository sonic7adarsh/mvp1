[build]
  # Publish directory relative to the base directory
  publish = "dist"
  
  # Run the build script (works for both Root and Subdir base settings)
  command = "bash netlify-build.sh || bash apps/seller-web/netlify-build.sh"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
