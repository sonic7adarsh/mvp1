# Shared Package

This folder can host shared modules (types, API helpers) used by web and mobile. For MVP v1, the web app includes the API client inline. If needed, move `apps/web/src/api` into a shared package and consume from both apps.